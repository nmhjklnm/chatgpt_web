build:
  base:
    context: .
#   api:
#     context: api
#   frontend:
#     context: frontend
#     dockerfile: Dockerfile
#     target: dev
#     depends_on: base
#     args:
#       SOURCE_IMAGE: ${OKTETO_BUILD_BASE_IMAGE}
#     secrets:
#       npmrc: .npmrc
deploy:
  - where python
  - pip show list
dev:
  api:
    command: ["bash"]
    forward:
      - 8080:8080
#   frontend:
#     command: streamlit run main.py

